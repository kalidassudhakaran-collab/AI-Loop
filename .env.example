# Copy this file to .env and fill in values locally.
# Never commit .env — Git ignores it so you can push without keys.

# Database
DATABASE_URL=postgresql://loop_user:loop_password@localhost:5433/loop?schema=public

# Auth.js / NextAuth
# ADD locally: generate a random 32+ character secret. Do not commit the real value.
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Anthropic Claude API (classification, Ask LOOP answers, VoC report narrative)
# ADD API: paste your Anthropic key here after copying to .env. Leave blank in git.
# Get a key at https://console.anthropic.com/ → API keys
ANTHROPIC_API_KEY=
# Optional. Defaults to claude-sonnet-4-6
ANTHROPIC_MODEL=

# Embeddings (optional, local Ollama — not a cloud API key)
# Supported: ollama | none
EMBEDDING_PROVIDER=
EMBEDDING_BASE_URL=http://127.0.0.1:11434
EMBEDDING_MODEL=nomic-embed-text

# Ask LOOP
ASK_LOOP_MIN_SIMILARITY=0.35
ASK_LOOP_DEBUG=
